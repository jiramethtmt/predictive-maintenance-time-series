pandas>=2.2,<3
numpy>=1.26
pyarrow>=15
scikit-learn>=1.4
# [all] drags in torch and the CUDA bindings, which fill the colima disk and are dead weight:
# the neural families are excluded from the search anyway.
autogluon.tabular[lightgbm,catboost,xgboost]==1.6.1
ray>=2.43.0,<2.57.0
